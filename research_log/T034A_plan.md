# T034-A / R052 frozen plan before model calls

Research edee06b / d18c1f9. Exact R052 ceiling audit only. No learned coefficients, finite ISP steps, AP, FCOS/SSD, prompt or deployment changes. T033 and prior scientific failures remain closed.

120 fresh train2017 images, selection seed 20261005, unchanged prepare_t018a eligibility and sha256(seed:ID) sorting. Exclude all 3791 prior/reserved IDs through T033 plus 5000 validation and 1400 evaluation IDs. Preparation reads annotations solely for the established outcome-free eligibility test; no task gradients or per-episode annotations are supplied to candidates. Full source annotations are opened by the reference process only after the 240-candidate commit/lock. IDs/JPEG hashes/provenance in T034A_train_cohort.json; candidate process receives only T034A_candidate_images.json. Cyclic gamma_s2/contrast_s2/color_cast_s2; four 30-pair blocks, 10 pairs/family/block. Clean then assigned corruption, 240 total episodes.

Precompute 256 permutations using NumPy 1.26.4 default_rng(20261005)/PCG64. Loop permutation, block 0..3, gamma/contrast/color_cast; within each stratum use ascending image-pair indices and rng.permutation(indices) as source task-pair indices for each candidate pair. Fixed points allowed; move clean/corrupt vectors together. T034A_permutations.json committed before models. Null 95th percentiles use NumPy linear interpolation. Retain all null episode values and overall/clean/corrupt/family/block median distributions.

Frozen source Faster R-CNN weight SHA256 258fb6c638b15964ddcdd1ae0748c5eef1be9e732750120cc857feed3faac384; state 73eed6eae3ab74a76539b3f76ff544ff19f7e9e06a6d7e20131ee4ece4751ecf. CLIP openai/clip-vit-base-patch32 revision 3d74acf9a28c67741b2f4f2ea7635f0aaf6f0268, pytorch_model.bin SHA a63082132ba4f97a80bea76823f544493bffa8082296d62d71581a4feff1576f; full semantic-loss state 6b38ac3696ff07a4e0cdbe436db05551707486e413df526256526b5777fe147c. Reuse exact T002/T009 positive/negative prompts, resize/crop, normalized features and semantic direction. Protected code hashes in T034A_protected_code_pins.json; actual new code hashes committed before execution.

All model work A6000 CUDA0, torch2.4.0+cu121, one torch CPU thread, setup/oracle seed20260913, native sampling seed20260930 with fork_rng restoration, cudnn benchmark false, deterministic algorithms false, no CUBLAS_WORKSPACE_CONFIG. Detector/CLIP frozen/eval, except accepted native scoped flags restored. Full state/RNG checks, same-cotangent direct ISP reverse parity cosine>=.999999 and relative L2<=1e-5. Preserve zeros and outliers. Literal native scalar authoritative; component-additivity and multi-output differences diagnostic only per R047.

Reuse map: pseudo_native_candidates.gradients supplies unchanged hard/native vectors and audited parity, native_task_signal supplies unchanged pseudo targets and literal four-component scalar. CLIP cotangent/JVP copies the small accepted gradient_state_candidates block using unchanged load_clip_guidance. roi_equivariance supplies CUDA setup, images, common_jvp and hashing. Reference loop reuses orthogonal_reference oracle/common-JVP logic after candidate lock. New code only handles 240-record orchestration, span/permutation math, focused tests and reporting. Same repository and environment; no external code added.

Candidate columns use exact g/||g|| for every nonzero gradient; exact zeros remain zero. For each HCN/HC/HN matrix, float64 thin SVD with rank tolerance eps64*max(shape)*smax; empty basis yields zero projection. Task u_t=t/(||t||+1e-12); zero task remains zero and counts in every denominator. C_H is unsigned abs(dot(u_h,u_t)); C_HCN=norm(Q.T@u_t); E=C_HCN-C_H. No clipping, basis selection, oracle coefficient use or candidate regeneration after reference reveal.

Frozen gates: median HCN>=.70 overall and corrupt, both strictly above respective null95; median E>=.10 overall and corrupt, both strictly above respective null95; at least 3/4 blocks median E>.05 and above its null95; rank>=2 in at least216/240 episodes; all integrity checks pass. Full conjunction only. PASS/FAIL stop NEEDS_REVIEW, integrity failure BLOCKED. No post-outcome changes.

Increment order: precommit cohort/plan/permutations/pins; donor baseline tests; candidate thin adapter and focused tests; math analytic/zero/null tests; reference lock/order tests and full regression; all240 candidates generated and committed plus descriptor BEFORE reference annotations; reference240 and frozen null; publish raw hashes/report and mirror to server. No duplicate experiments.

Preparation receipt: first command found an older shared/prepare_t018a.py via Python script-directory precedence and failed before cohort creation (unexpected count argument). Corrected command prepends the verified release scripts directory before runpy.run_path. No model calls or selection changes.
