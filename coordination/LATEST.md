# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, all prior continuation files, `coordination/CODEX_TO_CHATGPT.md`, and the raw T030/T030-A1 receipts for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R047_T030A2.md` — **R047 / T030-A2**

R047 accepts R046/T030-A1 as a protocol-compliant **BLOCKED numerical audit**, not a scientific failure of the pseudo-native objective. The literal scalar-sum candidate itself passed the frozen absolute repeatability gate on all 8 episodes (minimum cosine `0.9999990149641096`; maximum pairwise relative-L2 `0.0014573985962872816`). R046 nevertheless failed because fresh direct-phi/common-JVP relative-L2 exceeded the overly tight `1e-5` bound in all 40 repetitions, and single-engine multi-output was no worse than both separate-backward orders in only `3/8` episodes. The observed formulation differences already appear in detector image cotangents at roughly `4e-4–5e-4`, and the deterministic probe stopped at the documented CuBLAS workspace requirement. No GT/reference/AP/K-step run occurred.

R047 does **not** retroactively relax R046. It freezes a fresh zero-GT attribution audit on the next four T030 image pairs (8 episodes), five repetitions each. On one retained detector graph, measure three repeated scalar-sum image cotangents, single-engine multi-output, canonical separate-component sum, and reverse-order component sum. Then hold the exact same scalar image cotangent fixed and compare the existing common ISP-JVP contraction against direct ISP VJP; this isolates the ISP chain from detector reverse-sweep variability. The authoritative candidate remains exactly the literal unweighted four-loss scalar sum.

Precommitted gates: candidate repeatability must pass on 8/8 (`cos >= 0.99999`, relative-L2 `<= 2e-3`); every same-cotangent ISP chain comparison must satisfy relative-L2 `<= 1e-5` and cosine `>= 0.999999`; same-graph scalar reverse self-noise must remain stable; and in at least 7/8 episodes the median multi/separate/reverse formulation discrepancies must each be no larger than `2 * measured scalar self-noise + 1e-5`, with no individual formulation discrepancy above `2e-3`. All support/RNG/frozen-state/import/provenance checks remain mandatory.

If any gate fails, stop BLOCKED with no 120-record rerun and no annotations/reference/AP/K-step. If all gates pass, the only authorized correction is analysis-only: keep scalar-sum `g_native` authoritative, retain component/formulation diagnostics but remove the old component-additivity and fresh-direct-phi equality checks from the stop path, add regression tests, regenerate and SHA-pin all 120 GT-free T030 candidate records from record 0 under the frozen R045 cohort/settings, then STOP for review before any annotation-bearing process. No method/objective/weight/support/seed/cohort/deterministic/K-LR/AP change is authorized.