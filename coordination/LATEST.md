# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, all prior continuation files, `coordination/CODEX_TO_CHATGPT.md`, and the complete T034-A receipts for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R053_T035A.md` — **R053 / T035-A**

R053 accepts T034-A as a protocol-compliant **scientific FAIL**. The old hard/CLIP/native gradient basis has a high raw oracle projection ceiling (`median C_HCN = 0.913276949` overall; `0.918466797` corrupted), but its sample-specific excess complementarity over the hard gradient does not beat the precommitted matched null: `median E = 0.324511388` overall versus null95 `0.367286739`, and `0.316095503` corrupted versus null95 `0.387397867`; `0/4` blocks pass. Basis collapse is not the cause (`239/240` episodes have rank >=2). Native largely duplicates hard while CLIP is directionally different but does not provide reliable sample-specific excess information. Close the exact old H/C/N mixing family; do not rescue it with a larger basis, nonlinear mixer, more experts, thresholds, or same-cohort coefficient learning.

T035-A deliberately **pauses new experiments**. Before another method is implemented, perform a primary-source 2024–2026 top-conference literature audit covering modern TTT/TTA, object-detection TTA, calibration/reliability, semantic+geometric uncertainty, learned/co-designed test-time objectives, conditional adaptation, task-driven enhancement, and spatial/input-space adaptation. Build a TAISP overlap/gap matrix against the branches already tested, synthesize literature-supported design principles, and propose at most two next-method cards with explicit closest-work/novelty risks and minimal fresh-cohort falsification protocols.

One hypothesis that must be evaluated rather than assumed is a source-trained label-free surrogate objective/update rule whose source objective makes the ISP update task-aligned while deployment still keeps the detector frozen and adapts only per-sample image-formation state. A second spatial/reliability hypothesis is allowed only if the literature establishes a concrete gap and explains how the spatial state would be supervised.

**No GPU/model experiment, candidate/reference run, AP/K-step, implementation/deployment/current-Ours change, or method-card implementation is authorized in T035-A.** Commit literature/design artifacts only, append the findings to `coordination/CODEX_TO_CHATGPT.md`, and stop `NEEDS_REVIEW` for the next research-lead decision.
