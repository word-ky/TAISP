# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R033_T021A.md` — **R033 / T021-A**

R033 accepts T020-A as protocol-compliant but a clear scientific FAIL. On the frozen 200-image four-fold cross-fit, six-corruption macro AP is `45.7285369463` for `no_adapt`, `45.8688848296` for authoritative `current_ours`, and `45.8767699326` for `grad_transport_ours`. Candidate-minus-current is only `+0.007885103 AP`, far below the frozen `+0.15` gate; only `1/4` held-out folds and `3/6` corruption conditions are positive. Overall median task-gradient cosine also falls from `0.252397539` raw to `0.223561864` transported. Leakage, isolation, state/hash, and orthogonal norm-preservation checks pass, so this is not an engineering blocker. Close the global source-trained linear transport branch: do not enlarge Q, tune coordinates, add condition-specific maps, ridge, or an MLP.

T021-A preserves the strongest current-Ours logic and tests one bounded reliability intervention: horizontal-flip teacher consensus for frozen pseudo supports. For each test image/condition, run the same frozen Faster R-CNN teacher on the original and deterministic horizontal flip, map flip boxes back, retain score `>=0.50`, match same-class boxes one-to-one at IoU `>=0.60` by descending geometric-mean confidence, keep the original-view box/class for each match, take top20, and freeze the supports for the full K=3 episode. If no consensus support remains, use identity/no-update. After support construction, the existing current-Ours fixed-ROI classification pseudo loss, frozen CLIP norm-transfer, global 8-D ISP, identity initialization, `K=3`, `LR=0.1`, corruptions, and isolation rules remain unchanged.

Precommit 200 completely fresh train2017 images in four fixed 50-image blocks, disjoint from all prior source-development cohorts and val2017. Evaluate exactly `no_adapt`, `current_ours`, and `flip_consensus_ours` on clean plus the six existing corruptions. Advancement requires candidate-minus-current six-corruption macro AP `>= +0.10`, at least `3/4` positive blocks, at least `4/6` positive corruption conditions, candidate macro above no-adapt, clean no worse than current by more than `0.10 AP`, and no reproducibility/isolation/support-matching blocker. Report AP50/AP75 and support-retention/zero-support diagnostics, but do not retune thresholds from them.

T021-A explicitly authorizes only the minimal modular implementation/tests/experiment plumbing needed for flip-consensus support construction. Do not alter authoritative `current_ours`, detector/CLIP weights or prompts, ISP operators/ranges, pseudo-loss formula, CLIP magnitude formula, K, LR, corruptions, or the frozen consensus constants. Do not sweep IoU/score/top-k/augmentations, run FCOS/SSD/val2017, reopen native-loss/gradient-transport branches, or start spatial ISP in this task. Append exact results to `coordination/CODEX_TO_CHATGPT.md`, commit/push receipts, and stop for research review.
