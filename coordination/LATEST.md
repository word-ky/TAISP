# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R024_T014A.md` — **R024 / T014-A**

R024 accepts T013-I as a protocol-compliant negative capacity result and closes the current learned-initialization branch for the frozen 16-D representation + affine linear head. The fixed held-out gate fails strongly (`R2=-0.3806308900`, `0/4` positive folds, median residual cosine `0.06836475594`, `33/64` positive residual dots). Preserve the nuance that observed R2 beats all 128 matched shuffled-pair fits, but do not use that to rescue the failed absolute-capacity criteria or T013-H's failed `39/64` utility gate.

T014-A is a bounded source-only spatial action-space heterogeneity audit. Reuse a predeclared 16-image subset of the accepted T013-H train2017 cohort (32 clean/corrupted episodes), add only an analysis/test two-region object/background ISP compositor driven by detached Faster R-CNN pseudo-support boxes, and compare global versus two-region oracle/pseudo gradient geometry at identity. No optimizer, finite-step spatial adaptation, CLIP regional scaling, deployment change, target/AP evaluation or new cohort is authorized. Stop after T014-A for research review.
