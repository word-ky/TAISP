# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, all prior continuation files, `coordination/CODEX_TO_CHATGPT.md`, and the complete T033-A receipts for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R052_T034A.md` — **R052 / T034-A**

R052 accepts T033-A as a protocol-compliant **scientific FAIL** and closes the exact `confidence-pooled own-ROI descriptor -> PCA16 -> affine tangent correction` family on the revealed holdout. T033 respected the required ordering: all 600 label-free hard/ROI candidate records were locked before source supervision; the train-only PCA/state was locked before train task gradients; the affine model was locked before holdout corrected directions; all 120 corrected directions were locked before holdout annotations. Candidate/train/holdout integrity, state, RNG and common-JVP checks passed; AP/K-step remained zero; detector/ISP/current-Ours/deployment behavior was unchanged.

The frozen gate failed: `S_obj>0 = 60/120` overall and `34/60` corrupted; `Delta>0 = 65/120` overall and `32/60` corrupted; only `2/4` block medians were positive. Overall/corrupted mean and median Delta are slightly positive, but the effects are tiny and do not rescue the failed conjunction. The fitted affine design is also extremely ill-conditioned (condition about `1.90e7`, coefficient Frobenius about `4.85e5`, one almost-null singular direction), which is a warning against interpreting the learned correction as stable. Do not rescue T033 by ridge/TSVD/PCA/pooling/MLP changes on the same holdout.

T034-A asks a more fundamental question before any richer predictor: do the existing deployment-visible **hard pseudo, generic CLIP, and pseudo-native** ISP gradients jointly span the true task direction better than hard alone, beyond a matched permutation null? Use a completely fresh 120-image/240-episode train2017 cohort balanced across the three established severity-2 corruption families. Generate and SHA-lock all three 8-D label-free gradients for all episodes before opening annotations. Then compute the reference task gradient and the oracle projection ceiling of the task direction onto the full H+C+N span, plus hard-only/nested diagnostics.

Use exactly 256 pair-preserving permutations within each `(block, corruption-family)` stratum to establish the chance 3-D-subspace null while preserving clean/corrupt structure. The predeclared advancement gate requires median full-span projection `>=0.70` overall and corrupted, both above the 95th-percentile permutation-median null; median extra complementarity over unsigned hard alignment `>=0.10` overall and corrupted and above its 95th-percentile null; at least `3/4` blocks must show `E>0.05` above their null; at least 90% of episodes must have basis rank >=2; all integrity/leakage checks must pass.

This is an oracle **ceiling audit only**. No learned coefficient model, router, MLP, finite ISP step, AP, K-step runtime, FCOS/SSD, prompt change, component weighting or deployment-method modification is authorized. If the basis passes, stop for review before learning coefficients. If it fails, close this exact old-gradient-basis mixing direction and pivot to a genuinely new task-aligned scalar signal/state rather than another post-hoc transformation of the same gradients.