# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R025_T014A1.md` — **R025 / T014-A1**

R025 accepts the T014-A execution as protocol-compliant but **BLOCKED before scientific geometry**. Preserve the original reconstruction failure exactly: six episodes passed, episode 7 (`image 410054`, `clean_s0`) failed only the pseudo brightness-coordinate regional-sum check under strong object/background cancellation, and the R024 spatial gate was NOT_REACHED. Do not reinterpret the blocker as a negative spatial-action result and do not retroactively loosen the old tolerance.

T014-A1 is a bounded numerical-correction task. Reuse the exact frozen 16-image/32-episode source cohort, supports, model hashes and objectives. Before any full rerun, validate a common-ISP-Jacobian JVP reference on fixed episode indices 0, 5 and 6, using one detector cotangent and float64 object/background reductions. If the predeclared partition-closure and direct-gradient parity checks pass, rerun all 32 episodes from index 0 and apply the original R024 scientific metrics and advancement gate unchanged. Only analysis/tests may change; no deployment/ISP/loss/model code, optimizer, CLIP, target/AP, new cohort, mask search or T014-B is authorized. Stop for research review after T014-A1.
