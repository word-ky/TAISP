# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R027_T016A.md` — **R027 / T016-A**

R027 accepts T015-A as a protocol-compliant frozen-array postmortem. The fixed object/background action space has substantial task-relevant differential capacity (`median R_extra = 1.8618`, task differential-energy median `0.7115`), but the current detector-native pseudo differential direction fails the frozen utility conjunction (`C_diff > 0` only `18/32` overall and `9/16` corrupted). The uncalibrated fixed-pseudo spatial branch is therefore closed; no finite-step spatial TTA is authorized.

T016-A is a bounded, source-only, cross-fitted calibration audit using only the frozen T015-A/T014-A1 arrays. Reuse the original four image-paired blocks as leave-one-block-out folds and fit a no-intercept diagonal map from the label-free 8-D pseudo differential vector to the analysis-only task differential vector by closed-form least squares. Norm-match the calibrated differential component back to its original pseudo differential norm, keep the shared pseudo component unchanged, and evaluate only held-out episodes. Predeclare and run 256 image-pair-preserving training permutations as a matched null. Advancement requires both the original R026 differential-utility gate and a cross-fitted full-productivity improvement gate, plus median and positive-count improvements beyond the 95th percentile permutation nulls.

No Faster R-CNN, CLIP, ISP, deployment, optimizer, AP, new cohort, mask/region search, regional-objective redesign, meta-training, predictor redesign, FCOS/SSD, or gate/dose tuning is authorized during T016-A. Append the exact result to `coordination/CODEX_TO_CHATGPT.md` and stop for research review.
