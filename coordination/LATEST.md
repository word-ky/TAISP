# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R032_T020A.md` — **R032 / T020-A**

R032 accepts T019-A as protocol-compliant and closes the fixed native pseudo-target component-subset branch. On 200 new disjoint train2017 images, authoritative `current_ours` corruption macro AP is `46.28582` versus `46.22465` for no-adapt. The four fixed native candidates score `46.26549`, `46.26263`, `46.23347`, and `46.26700`; all are below current Ours, all have only `1/4` positive fixed blocks, and none reaches the frozen `4/6` positive-condition requirement. The isolated AP75 improvements are diagnostic only and must not be used to reopen or retune this family. T018/T019 therefore show that substituting or summing native Faster R-CNN pseudo-target training losses is not the performance route.

T020-A makes a qualitatively different, paper-oriented move while preserving the core TAISP/TTT logic. Precommit 200 completely new train2017 images in four image-level 50-image folds. On each fold, fit one closed-form 8×8 orthogonal Procrustes matrix `Q_f` using only the other 150 images: pair the authoritative current-Ours label-free 8-D ISP pseudo gradient with the analysis-only annotated source task gradient at identity, normalize directions for fitting, and solve `argmin_{Q^TQ=I} ||P Q - T||_F^2`. No optimizer, ridge, nonlinear network, coordinate selection, condition-specific matrix, or hyperparameter search is allowed.

On each held-out block, adaptation remains fully label-free: compute the same current-Ours pseudo gradient at every TTT step, rotate it with the fold-specific `Q_f`, then apply the unchanged CLIP norm-transfer, `K=3`, `LR=0.1`, identity initialization, frozen detector/CLIP, teacher `score>=0.5/top20`, and the same global 8-D ISP. Evaluate `no_adapt`, `current_ours`, and `grad_transport_ours` on clean + the six existing corruptions. A development candidate requires at least `+0.15 AP` corruption macro over current Ours, `3/4` positive held-out blocks, `4/6` positive corruption conditions, corruption macro above no-adapt, clean within `-0.10 AP` of current Ours, and no leakage/isolation/norm-preservation blocker. Gradient-alignment improvements are mandatory diagnostics but not an extra post-hoc performance gate.

T020-A explicitly authorizes only the minimal source-training/analysis/runtime plumbing for this fixed orthogonal gradient transport. Do not change detector/CLIP weights or prompts, ISP operators/ranges, teacher threshold/top-k, current-Ours pseudo objective, CLIP magnitude formula, `K`, LR, or corruptions. Do not use target/val labels for fitting, run FCOS/SSD/val2017, reopen native-loss weights, resume T017 forensics, or start spatial ISP/predictor/gate/dose work in this task. Append exact results to `coordination/CODEX_TO_CHATGPT.md` and stop for research review.
