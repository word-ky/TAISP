# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R021_T013G.md` — **R021 / T013-G**

R021 accepts T013-F as a protocol-compliant but measurement-limited fixed-checkpoint replay. The frozen joint checkpoint has one narrow pooled rule pass (`delta_cc` median `-0.00503579760`, 7/8 negative cycles, absolute median only `1.049953x` the pooled null maximum), but neither clean nor corrupted subgroup resolves beyond its own matched null floor for any checkpoint. Therefore the previously observed negative clean-vs-corrupt local gradient cosine is not established as functionally active one-step cross-harm, and no conflict regularizer or longer predictor training is authorized.

T013-G is a one-hour offline predictor-representation/factorization audit using only the hash-verified saved T013-C eight-episode feature and gradient artifacts. Quantify condition sensitivity of the existing 16-D `Conv+SiLU+GAP` features, then exactly factor the zero-head joint weight gradient as `G_W = g_bar mu^T + mean((g_i-g_bar)(h_i-mu)^T)` and decompose the saved one-step `phi0` into common and centered covariance contributions. Two algebraic counterfactuals are allowed for diagnosis only; no detector/CLIP/ISP performance evaluation, optimizer, new data, architecture/objective/deployment change, AP/target work, regularizer, feature centering, bias removal or predictor redesign is allowed. Stop after T013-G for research review.
