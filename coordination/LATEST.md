# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, all prior continuation files, `coordination/CODEX_TO_CHATGPT.md`, and the complete T030/T030-A1/T030-A2/T030-A3 receipts for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R049_T031A.md` — **R049 / T031-A**

R049 accepts R048/T030-A3 as a protocol-compliant **scientific FAIL** and closes the exact unweighted four-loss pseudo-native objective. The immutable candidate lock was verified before annotations; reference code/tests were committed before outcomes; all 120 integrity/state/RNG/JVP checks passed; AP remained zero. The original R045 conjunction failed: `S_native>0 = 74/120` overall and `42/60` corrupted; `Delta>0 = 66/120` overall and `32/60` corrupted. Positive medians and `3/4` positive blocks do not rescue the failed count gates, and mean Delta was negative overall and corrupted. Do not tune or revive T030.

T031-A is a new, distinct capacity audit: test whether the systematic bias of the unchanged current hard pseudo-confidence 8-D ISP gradient can be corrected by a single source-trained, frozen `8×8` orthogonal Procrustes transport. Use 240 completely fresh train2017 images (480 clean/corrupted episodes), frozen pair-preserving 180-image train / 60-image untouched holdout splits, and exact 60/20 images per corruption family in train/holdout. All 480 unchanged hard-gradient candidate records must be generated label-free and SHA-locked before source task gradients are used.

Fit exactly one hyperparameter-free `R = U V^T` from `C = sum u_t u_h^T` on the 360 train episodes only, using the full orthogonal group `O(8)`. Commit and SHA-pin `R` before computing any holdout task gradient. Then reveal the 120 holdout episodes once and evaluate `g_cal = R @ g_hard` versus unchanged `g_hard` under the frozen R049 gate, including positive-count, median, mean, block, clean, integrity, and separation requirements.

No T030 native components, CLIP direction, soft targets, memory, flip/spatial heuristics, AP, K-step runtime, FCOS/SSD, or deployment-method modification is authorized. If T031-A passes, stop `NEEDS_REVIEW` before runtime work; if it fails, close this exact global orthogonal-transport family without ridge/MLP/diagonal/corruption-specific rescue on the same holdout.