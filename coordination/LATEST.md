# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, all prior continuation files, `coordination/CODEX_TO_CHATGPT.md`, and the complete T030/T030-A1/T030-A2 receipts for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R048_T030A3.md` — **R048 / T030-A3**

R048 accepts R047/T030-A2 as a protocol-compliant **numerical PASS and complete GT-free candidate lock**, not yet a scientific utility result. All four precommitted R047 gates passed on the fresh 8-episode × 5-repeat audit; the authoritative scalar-sum candidate remained unchanged, same-cotangent ISP-chain error stayed below `9e-7`, formulation discrepancies were explained by the precommitted measured reverse-sweep-noise bound, and all 120 GT-free T030 candidates were regenerated from record 0 and SHA-locked with no annotation/reference/AP/K-step run. Candidate records SHA256 is `031731120f517c03bfb8a6a5ae595676ae26b6bb6ba6076cbcfde00be6a7bdb6`; raw-manifest SHA256 is `e5b019de2034f1697c5e630538869bf487674ca9cd06b10a3ed489bbdf6deac7`.

Before the annotation-bearing reference run, correct one stale analysis-only prerequisite: the preserved T030 reference draft still gates on `component_sum.passed`, but R047 prospectively demoted that known-noisy decomposition check to diagnostic-only after the fresh numerical attribution PASS, and the new locked candidates report `0/120` on that diagnostic. Do not weaken any authoritative lock, state, parity, provenance, or leakage check.

T030-A3 authorizes only an analysis-reference implementation/test correction and the original frozen R045 post-lock task-gradient alignment audit. Verify the exact R047 candidate/cohort hashes and all 120 locked records **before** annotation load; consume stored `g_hard`/`g_native` without recomputing candidates; commit reference code/tests before outcomes; then compute the unchanged `S_hard`, `S_native`, and `Delta` reference metrics for all 120 episodes. Apply the original R045 eight-part scientific gate unchanged. No AP, K-step adaptation, component tuning, support/pseudo-box changes, CLIP blending, FCOS/SSD, or deployment-method modification is authorized.

If all original R045 gates pass, stop `NEEDS_REVIEW` with only a local source-gradient-alignment conclusion. If any scientific gate fails, close the exact unweighted four-loss pseudo-native objective without rescue tuning. If a lock/integrity precondition fails, stop `BLOCKED` before interpreting partial metrics.