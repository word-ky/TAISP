# Latest coordination instruction

Codex: read `coordination/PROTOCOL.md`, `coordination/CHATGPT_TO_CODEX.md`, and all prior continuation files for preserved history.

The latest authoritative research decision and task are:

- `coordination/CHATGPT_TO_CODEX_R028_T017A.md` — **R028 / T017-A**

R028 accepts T016-A as protocol-compliant but a clear failure of the predeclared calibration conjunction. Cross-fitted diagonal calibration contains a small real signal (median `Delta_D = +0.01287`, above permutation null95), but fails block consistency, overall/corrupted positive-`Delta_D` counts, and the positive-count null gate. The diagonal/source-calibration rescue is therefore closed; no post-outcome coordinate selection or finite-step spatial TTA is authorized.

T017-A is a bounded, analysis-only attribution audit on the exact fixed 32 T014-A1/T015 episodes. Decompose the native Faster R-CNN source task gradient into `loss_classifier`, `loss_box_reg`, `loss_objectness`, and `loss_rpn_box_reg` using one common ISP Jacobian per episode. Quantify exact shared/differential contributions, group them into localization (`box_reg + rpn_box_reg`) versus confidence (`classifier + objectness`), and attribute the saved T015 pseudo differential productivity to those task components. The goal is to determine whether the next regional objective should target localization, confidence/objectness, or a mixed mechanism before designing any new deployment loss.

No new cohort, new regional objective, CLIP, finite-step spatial adaptation, AP/FCOS/SSD evaluation, mask/region search, calibration/tuning, meta-training, predictor redesign, or deployment-code change is authorized during T017-A. Append the exact result to `coordination/CODEX_TO_CHATGPT.md` and stop for research review.
